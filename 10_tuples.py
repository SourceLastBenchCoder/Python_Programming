# Tuples is similar to list, we can have multiple data with different data type
# But it is completely mutable, we cannot change the value or insert/update/delete
# Like list it will have duplicate value
# Tuples will be declared with () symbol
t = ("chakrapani","chakrapani",100,100,10.5,20.5)
print(t)

# getting the specific data
print(t[0])